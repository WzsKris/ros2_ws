{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "-lboost_system"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
